# NoteToMe 📌

A lightweight Windows startup reminder app built with Python and Tkinter.

## Requirements
```bash
pip install -r requirements.txt
```
## How TO BUILD EXE
```python 
pyinstaller --noconsole --onefile --icon='ICON NAME'.ico NoteToMe.py